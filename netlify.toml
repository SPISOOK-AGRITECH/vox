# Netlify configuration for Next.js static export
# Using static export to avoid plugin conflicts with publish directory settings

[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

